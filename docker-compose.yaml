services:
  raquel_portifolio:
    container_name: raquel-portifolio
    env_file:
    - .env
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${PORT}:${PORT}"